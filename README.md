# Installation

Clone the repo to any location. Recommend your home directorty, `~` or `$HOME`.
```sh
git clone --bare https://github.com/mswinford/config.git ~/.cfg
```

Checkout the branch you need
```sh
git checkout --git-dir=~/.cfg --work-tree=~ checkout main
```
